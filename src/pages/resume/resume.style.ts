import { createUseStyles } from "react-jss";

const styles= {
    resume:{
        // marginTop: '50px',
        // backgroundColor:'white',
        // borderRadius:'15px',
        padding:'50px',
    }
};
export const useResumeStyles = createUseStyles(styles);
